[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app fallback (not strictly needed for a one-pager but harmless)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
